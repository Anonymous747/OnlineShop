enum ExsoColor {
  textOnInteraction,
  textPrimary,
  textSecondary,
  primaryHeader,
  buttonText,
  selectedPrimaryText,
  primaryButton,
  detailsBackground,
  semiTransparentBackground,
  transparent,
}
