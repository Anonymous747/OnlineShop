import 'package:flutter/material.dart';

class LocationTitle extends StatelessWidget {
  const LocationTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Location title'),
    );
  }
}
